<%= form_with(model: phone_supplier) do |form| %>
  <% if phone_supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(phone_supplier.errors.count, "error") %> prohibited this phone_supplier from being saved:</h2>

      <ul>
        <% phone_supplier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :telefono %>
    <%= form.text_field :telefono %>
  </div>

  <div class="field">
    <%= form.label :supplier_id %>
    <%= form.text_field :supplier_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
